/** Public homepage for the developer / product. */
export const DEVELOPER_URL = "https://helvety.com";

/**
 * Shown under {@link DEVELOPER_NAME} in the popup header. Must match `name` in
 * `public/manifest.json` (see `tests/constants.test.ts` manifest drift guard).
 */
export const EXTENSION_DISPLAY_NAME = "Power Automate Editor Version Enforcer" as const;

/** Display name shown in the About tab. */
export const DEVELOPER_NAME = "Helvety";

/** Public Git repository for this extension (issues, PRs). Slug: `power-automate-editor-version-enforcer`. */
export const SOURCE_REPO_URL =
  "https://github.com/CasparRubin/power-automate-editor-version-enforcer";
